---
/**
 * ProductCard — WellPeps hair restoration product-card style.
 *
 * Original approved design: image (with a small icon badge) · title ·
 * description · "Common options include" list · "Plans from $X/month" ·
 * outlined "Start Your Free Assessment" button.
 */
import Icon from './Icon.astro';
import type { Product } from '../data/hair';

interface Props {
  product: Product;
}
const { product } = Astro.props;
const {
  name, image, alt, icon, description, optionsLabel, options,
  methodOfUse, price, priceUnit, href,
} = product;
---
<article class="pcard">
  <div class="pcard__media">
    <img src={image} alt={alt} width="720" height="540" loading="lazy" />
  </div>

  <div class="pcard__body">
    <h3 class="pcard__name">{name}</h3>
    <p class="pcard__desc">{description}</p>

    <div class="pcard__options">
      <p class="pcard__options-label">{optionsLabel}</p>
      <ul>
        {options.map((o) => <li>{o}</li>)}
      </ul>
    </div>

    <div class="pcard__method">
      <p class="pcard__method-label">Method of Use</p>
      <p class="pcard__method-value">{methodOfUse}</p>
    </div>

    <p class="pcard__price">
      <span class="pcard__price-label">Starting at</span>
      <span class="pcard__price-row">
        <span class="pcard__price-amount">${price}</span>
        <span class="pcard__price-unit">{priceUnit}</span>
      </span>
    </p>

    <a href={href} class="pcard__cta">Start Free Assessment <Icon name="arrow-right" /></a>
  </div>
</article>

<style>
  .pcard {
    display: flex; flex-direction: column;
    background: #fff;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    box-shadow: var(--shadow-sm);
    overflow: hidden;
    transition: transform var(--dur) var(--ease), box-shadow var(--dur) var(--ease);
  }
  .pcard:hover { transform: translateY(-6px); box-shadow: var(--shadow-lg); }

  .pcard__media { position: relative; aspect-ratio: 4 / 3; overflow: hidden; background: var(--bg-gray); }
  .pcard__media img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s var(--ease); }
  .pcard:hover .pcard__media img { transform: scale(1.03); }

  .pcard__body { display: flex; flex-direction: column; flex: 1; padding: 1.5rem 1.5rem 1.6rem; }

  .pcard__name {
    font-family: var(--font-serif);
    font-weight: 700; letter-spacing: normal;
    font-size: 1.4rem; color: var(--navy); line-height: 1.2;
  }
  .pcard__desc { margin-top: 0.7rem; color: var(--text-body); font-size: var(--text-card-body); line-height: 1.55; }

  .pcard__options { margin-top: 1.15rem; }
  .pcard__options-label { font-weight: 700; color: var(--navy); font-size: var(--text-body-size); }
  .pcard__options ul { list-style: none; padding: 0; margin: 0.55rem 0 0; display: flex; flex-direction: column; gap: 0.4rem; }
  .pcard__options li {
    position: relative; padding-left: 1.15rem;
    color: var(--text-body); font-size: var(--text-body-size); line-height: 1.45;
  }
  .pcard__options li::before {
    content: ''; position: absolute; left: 0; top: 0.5em;
    width: 6px; height: 6px; border-radius: 50%; background: var(--blue-soft);
  }

  /* Method of use — its own band, separated by hairlines as in the mockup. */
  .pcard__method {
    margin-top: 1.15rem; padding-top: 1.1rem;
    border-top: 1px solid var(--border);
  }
  .pcard__method-label { font-weight: 700; color: var(--navy); font-size: var(--text-body-size); }
  .pcard__method-value { margin-top: 0.2rem; color: var(--text-body); font-size: var(--text-body-size); }

  /* Price + CTA pinned to the bottom so cards align across the row.
     Scale is shared with every other product card via the price tokens. */
  .pcard__price {
    margin-top: auto; padding-top: 1.1rem;
    border-top: 1px solid var(--border);
  }
  .pcard__price-label { display: block; color: var(--text-muted); font-size: var(--price-label); }
  .pcard__price-row { display: flex; align-items: baseline; gap: 0.35rem; margin-top: 0.1rem; }
  .pcard__price-amount { font-family: var(--font-sans); font-weight: 800; letter-spacing: -0.02em; font-size: var(--price-amount); color: var(--navy); line-height: 1; }
  .pcard__price-unit { color: var(--text-muted); font-size: var(--price-unit); }

  /* Filled CTA per the WellPeps Product Card Standard: Ocean Boat Blue,
     white text, 48px tall, 16px radius. */
  .pcard__cta {
    position: relative;
    display: flex; align-items: center; justify-content: center;
    margin-top: 1rem; width: 100%;
    min-height: 48px; padding: 0.75rem 2.6rem;
    /* The arrow is absolutely positioned, so the label must not wrap into it. */
    white-space: nowrap;
    background: var(--blue); border-radius: 16px;
    color: #fff; font-family: var(--font-sans); font-weight: 600; font-size: var(--text-body-size);
    text-align: center; line-height: 1.3;
    transition: background var(--dur) var(--ease);
  }
  .pcard__cta :global(svg) { position: absolute; right: 1.1rem; width: 1.05em; height: 1.05em; transition: transform var(--dur) var(--ease); }
  .pcard__cta:hover { background: var(--blue-hover); color: #fff; }
  .pcard__cta:hover :global(svg) { transform: translateX(3px); }
</style>
