# Copy to .env and fill in. .env is gitignored.
#
# The site is a STATIC build: these are read by `astro build` only and are
# never shipped to the browser (note the deliberate lack of a PUBLIC_ prefix).

SUPABASE_URL=https://kwgwbupqzpusydzflyvi.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_43byAuuqBPNUr0I4-_1jDQ_GyClnOlU

# Needed ONLY to seed/refresh article content (npm run seed:articles).
# Bypasses RLS — never commit it, never expose it to the browser.
SUPABASE_SERVICE_ROLE_KEY=
